/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['192.168.1.105', 'localhost:3000'],
  experimental: {
    /* experimental options here */
  }
};

export default nextConfig;
